source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "jekyll-remote-theme"
# (opcional) plugins extras que você usa
# gem "jekyll-seo-tag"

# Para dev local com `bundle exec jekyll serve`
group :development do
  gem "webrick"
end
